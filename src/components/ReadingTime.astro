---
interface Props {
  minutes: number;
}

const { minutes } = Astro.props;

const coffeeCount = Math.max(1, Math.floor(minutes / 5));
---

{new Array(coffeeCount).fill(0).map(() => "☕")}{" "}{Math.floor(minutes)} min read
